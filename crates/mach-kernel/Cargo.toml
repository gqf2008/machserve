[package]
name = "mach-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "MachServe kernel boundary: Kernel trait, op registry and ops/<family>/<solution>."

[dependencies]
mach-engine = { workspace = true }
thiserror = "2"

[lints]
workspace = true
