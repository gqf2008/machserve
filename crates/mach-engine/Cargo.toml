[package]
name = "mach-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "MachServe core: HIP graph capture lifecycle (slated for removal in backend refactor batch 3)."

[features]
default = []
# ROCm/HIP backend: graph capture on AMD GPUs.
hip = ["dep:mach-kernel-sys", "mach-kernel-sys/hip"]

[dependencies]
mach-kernel-sys = { workspace = true, optional = true }
thiserror = "2"

[lints]
workspace = true

