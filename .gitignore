/target
Cargo.lock
*.o
*.a
*.so
*.dylib
*.dll

# downloaded test models (fetch with the commands in tests/real_model.rs)
/.models

__pycache__/

# local scratch: A/B scripts, server logs, one-off inspection tools
/.scratch

# junk guards: shell that fails to expand %SystemDrive%, throwaway probes
/%SystemDrive%/
crates/mach-model/examples/ab_probe_tmp.rs

# C4 vision runbook outputs (golden/e2e dumps, never committed)
/artifacts
